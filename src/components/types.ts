export type userType = {
    id: number,
    login: string,
    password: string,
    name: string,
    surname: string,
};

export type bodyUserType = {
    login: string,
    password: string,
    name: string,
    surname: string,
};
